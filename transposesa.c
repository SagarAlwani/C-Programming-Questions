#include<stdio.h>
#define SIZE 10
	void main()
	{
		int i,j,saga[SIZE][SIZE],r,c;
		printf("Enter the No. of Rows\n");
		row:
		scanf("%d",&r);
			if(r>SIZE)
		{
			printf("Row must Not Exceed %d\n",SIZE);
			goto col;
		}
		printf("Enter the No. of Columns\n");
		else if
		{
			printf("Row must Not Exceed %d\n",SIZE);
			goto row;
		scanf("%d",&c);	
		}
		
	}

